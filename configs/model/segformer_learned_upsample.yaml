# @package model
# SegFormer with learned upsampling head (ConvTranspose2d) instead of bilinear interpolation.
# All other settings identical to segformer.yaml.
architecture: segformer
num_outputs: 1
in_channels: 3
outputs:
  roads: 0
variant: nvidia/segformer-b0-finetuned-ade-512-512
pretrained: true
learned_upsample: true
